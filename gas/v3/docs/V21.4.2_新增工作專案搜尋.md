# V21.4.2 新增工作專案搜尋

「新增工作」已改為表單內搜尋模式：

1. 在「搜尋並選擇專案」輸入至少 2 個字。
2. 專案結果以卡片方式直接展開在表單內，不是下拉選單。
3. 可依場地代號、名稱、客戶、負責人、系統或狀態搜尋。
4. 點選專案卡片後，再填寫工作標題與其他內容。

若新增工作視窗沒有顯示 `V21.4.2｜專案欄位為表單內搜尋，不使用下拉選單`，代表目前 `/exec` 網址仍指向舊部署版本，需在 Apps Script「管理部署作業」編輯現有部署並選擇新版本。
